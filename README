バイナリファイルの中身を出力するプログラム(double型)

オプション
-f ファイル名 ←必須
-l 出力する数
-e エンディアン変換のフラグ

仕様
PCがbig endian か little endianを出力
読み込んだ要素数(デフォルトは500)
でんディアン変換(デフォルトはしない)
ファイルの最後までいくと、そこまでを出力

sample.dat でテストできます(^_^)
